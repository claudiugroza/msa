# Write a script that controls the state of an LED using a push-button. 
# The LED should change its state only when the button is pressed 
# (warning: it should not react to button release). 
# The detection of an event is confined to the main thread.

import RPi.GPIO as GPIO
